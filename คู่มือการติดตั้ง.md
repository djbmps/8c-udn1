# คู่มือการติดตั้งและใช้งาน — ระบบเก็บข้อมูลทักษะ 8C

**สำนักงานเขตพื้นที่การศึกษาประถมศึกษาอุดรธานี เขต 1**

ใช้เวลาติดตั้งประมาณ 15–20 นาที ทำครั้งเดียว ไม่ต้องเขียนโค้ดเพิ่ม

> ⚠️ ทำทุกขั้นตอนด้วย **บัญชี Google ของเขตพื้นที่** ที่จะเป็นเจ้าของข้อมูลระยะยาว
> เพราะไฟล์ Sheet และ Script จะอยู่ใน Drive ของบัญชีนั้น

> 📌 หากระบบติดตั้งเรียบร้อยแล้ว ให้ข้ามไปที่หัวข้อ
> [**การใช้งานประจำ**](#การใช้งานประจำ) ได้เลย

---

## ภาพรวมของระบบ

| ชั้น | ไฟล์ | ใครใช้ | เข้าอย่างไร |
|---|---|---|---|
| 1 | `index.html` | ครูประจำชั้น | เปิดลิงก์ได้เลย ไม่ต้องใช้รหัส |
| 2 | `school.html` | ผู้บริหาร/ครูวิชาการของโรงเรียน | รหัสเฉพาะโรงเรียน (`UDN-xxxx-xxxx`) |
| 3 | `admin.html` | ศึกษานิเทศก์ / เขตพื้นที่ | รหัสผ่านผู้ดูแลระบบ |

ครูกรอกข้อมูลครั้งเดียวเป็นรายห้อง โรงเรียนและเขตได้ตัวเลขสรุปอัตโนมัติ

---

## ขั้นที่ 1 — สร้าง Google Sheet

1. เปิด <https://sheets.new> จะได้ไฟล์เปล่า 1 ไฟล์
2. ตั้งชื่อไฟล์ เช่น `ฐานข้อมูลทักษะ 8C สพป.อุดรธานี เขต 1`
3. ดูที่แถบ URL ด้านบน จะมีรูปแบบ

   ```
   https://docs.google.com/spreadsheets/d/1AbCdEf...XyZ/edit#gid=0
                                          └──── ส่วนนี้คือ SHEET_ID ────┘
   ```

4. **คัดลอกรหัสระหว่าง `/d/` กับ `/edit` เก็บไว้** — จะใช้ในขั้นที่ 3

ไม่ต้องสร้างแผ่นงานหรือหัวตารางเอง ระบบจะสร้างแผ่นงาน `ข้อมูลรายห้อง`
พร้อมหัวตาราง 63 คอลัมน์ให้อัตโนมัติเมื่อรันครั้งแรก

---

## ขั้นที่ 2 — สร้างโปรเจกต์ Apps Script

1. เปิด <https://script.google.com> → กด **โครงการใหม่ (New project)**
2. ตั้งชื่อโครงการ เช่น `8C Backend — สพป.อุดรธานี 1`
3. ลบโค้ดตัวอย่าง `function myFunction() {}` ที่มีอยู่ออกให้หมด
4. เปิดไฟล์ `Code.gs` ที่ได้รับ คัดลอก **ทั้งไฟล์** มาวางแทน
5. กด **บันทึก** (ไอคอนแผ่นดิสก์ หรือ Ctrl+S)

---

## ขั้นที่ 3 — แก้ค่าตั้งต้น 4 บรรทัด

ที่ส่วนบนของ `Code.gs` จะเห็น

```javascript
const SHEET_ID = 'ใส่รหัส_GOOGLE_SHEET_ที่นี่';
const ADMIN_PASS = 'udn1@8c2569';
const CODE_SALT = 'udn1-8c-salt-เปลี่ยนค่านี้ด้วย';
const OPEN_FOR_SUBMIT = true;
```

| ตัวแปร | ใส่อะไร |
|---|---|
| `SHEET_ID` | รหัสที่คัดลอกไว้จากขั้นที่ 1 |
| `ADMIN_PASS` | **เปลี่ยนเป็นรหัสผ่านของท่านเอง** สำหรับเข้าแดชบอร์ดเขต |
| `CODE_SALT` | **เปลี่ยนเป็นข้อความลับของท่านเอง** ใช้สร้างรหัสเฉพาะโรงเรียน |
| `OPEN_FOR_SUBMIT` | `true` = เปิดรับข้อมูล · `false` = ปิดรับชั่วคราว |

> ⚠️ **ห้ามเปลี่ยน `CODE_SALT` หลังแจกรหัสให้โรงเรียนแล้ว**
> เพราะรหัสเดิมของทุกโรงเรียนจะใช้ไม่ได้ทันที ต้องแจกใหม่ทั้งหมด

กด **บันทึก** อีกครั้ง

### ทดสอบการเชื่อมต่อ

1. ที่แถบเลือกฟังก์ชันด้านบน เลือก **`ทดสอบการตั้งค่า`**
2. กด **เรียกใช้ (Run)**
3. ครั้งแรกจะขออนุญาตเข้าถึงบัญชี — กด **ตรวจสอบสิทธิ์** →
   เลือกบัญชี → หากขึ้นหน้าจอ *"Google ไม่ได้ยืนยันแอปนี้"*
   ให้กด **ขั้นสูง (Advanced)** → **ไปที่ ... (ไม่ปลอดภัย)** → **อนุญาต**
   *(ปลอดภัย เพราะเป็นสคริปต์ที่ท่านวางเอง)*
4. ดูผลที่ช่อง **บันทึกการดำเนินการ (Execution log)** ต้องขึ้น

   ```
   ✅ เชื่อมต่อ Google Sheet สำเร็จ: ฐานข้อมูลทักษะ 8C ...
      • แผ่นงาน "ข้อมูลรายห้อง" (0 รายการ)
   ✅ ระบบเปิดรับข้อมูล
   ```

   เปิดไฟล์ Sheet ดู จะพบแผ่นงานพร้อมหัวตารางสีน้ำเงินแล้ว

---

## ขั้นที่ 4 — Deploy เป็นเว็บแอป

1. มุมขวาบน กด **ทำให้ใช้งานได้ (Deploy)** → **การทำให้ใช้งานได้ใหม่ (New deployment)**
2. กดไอคอนเฟือง ⚙️ ข้างคำว่า *เลือกประเภท* → เลือก **เว็บแอป (Web app)**
3. ตั้งค่าตามนี้ **(สำคัญมาก)**

   | ช่อง | ค่าที่ต้องตั้ง |
   |---|---|
   | คำอธิบาย | `8C v1` |
   | ดำเนินการในฐานะ (Execute as) | **ฉัน (Me)** |
   | ผู้ที่มีสิทธิ์เข้าถึง (Who has access) | **ทุกคน (Anyone)** |

   > ต้องเป็น **ทุกคน** เท่านั้น มิฉะนั้นครูที่ไม่ได้ล็อกอิน Google จะส่งข้อมูลไม่ได้

4. กด **ทำให้ใช้งานได้** → หากขึ้นหน้าขอสิทธิ์อีกครั้ง ให้กด **ให้สิทธิ์เข้าถึง**
   แล้วอนุญาตตามขั้นตอนเดิม
5. คัดลอก **URL ของเว็บแอป** ที่ลงท้ายด้วย `/exec`

   ```
   https://script.google.com/macros/s/AKfycb....../exec
   ```

---

## ขั้นที่ 5 — ใส่ URL ลงในไฟล์เว็บ

เปิด `index.html`, `school.html` และ `admin.html` ด้วยโปรแกรมแก้ไขข้อความ
(Notepad, TextEdit, VS Code — **ห้ามใช้ Microsoft Word**)

ค้นหาบรรทัดนี้ (อยู่ในส่วน `<script>` ท้ายไฟล์)

```javascript
const API_URL = "";
```

แก้เป็น

```javascript
const API_URL = "https://script.google.com/macros/s/AKfycb....../exec";
```

**ต้องแก้ทั้งสามไฟล์** แล้วบันทึก

---

## ขั้นที่ 6 — นำไฟล์เว็บขึ้นออนไลน์

เลือกวิธีใดวิธีหนึ่ง

### วิธี ก — GitHub Pages (แนะนำ ฟรี ถาวร)

1. สมัคร/เข้า <https://github.com> → **New repository** → ตั้งชื่อ เช่น `8c-udn1`
   เลือก **Public** → **Create**
2. กด **uploading an existing file** → ลาก `index.html`, `school.html`
   และ `admin.html` เข้าไป → **Commit**
3. ไปที่ **Settings → Pages** → ที่ *Branch* เลือก `main` / `root` → **Save**
4. รอ 1–2 นาที จะได้ลิงก์

   ```
   https://ชื่อผู้ใช้.github.io/8c-udn1/             ← ส่งให้ครูประจำชั้น
   https://ชื่อผู้ใช้.github.io/8c-udn1/school.html  ← ส่งให้โรงเรียน
   https://ชื่อผู้ใช้.github.io/8c-udn1/admin.html   ← สำหรับเขตพื้นที่
   ```

### วิธี ข — Google Sites

สร้างหน้าเว็บใน Google Sites แล้วฝังไฟล์ HTML ผ่านกล่อง **ฝัง (Embed) → รหัสฝัง**
เหมาะกับเขตที่มีเว็บไซต์ Google Sites อยู่แล้ว

### วิธี ค — ใช้แบบออฟไลน์

ส่งไฟล์ `index.html` ให้ครูดับเบิลคลิกเปิดในเบราว์เซอร์ได้เลย
ข้อมูลยังส่งเข้า Google Sheet ตามปกติ (ต้องมีอินเทอร์เน็ต)

---

## ขั้นที่ 7 — แจกรหัสเฉพาะโรงเรียน

โรงเรียนแต่ละแห่งมีรหัสประจำตัว เช่น `UDN-0101-6C0B` สำหรับเปิดแดชบอร์ดของตนเอง

**วิธีดูรหัสทั้งหมด**

- เปิด `admin.html` → ใส่รหัสผ่านผู้ดูแลระบบ → แท็บ **รหัสโรงเรียน**
  → กด **ส่งออก CSV** จะได้ไฟล์รายชื่อโรงเรียนพร้อมรหัส 214 รายการ
- หรือที่ Apps Script เลือกฟังก์ชัน **`พิมพ์รหัสโรงเรียน`** แล้วกด Run
  รหัสทั้งหมดจะแสดงใน Execution log

**สิ่งที่ควรบอกโรงเรียน**

> ลิงก์แดชบอร์ดโรงเรียน: `https://.../school.html`
> รหัสของโรงเรียนท่าน: `UDN-xxxx-xxxx`
> (หรือเปิดตรงด้วย `https://.../school.html?code=UDN-xxxx-xxxx`)
> รหัสนี้เห็นเฉพาะข้อมูลของโรงเรียนท่านเท่านั้น

---

## ขั้นที่ 8 — ทดสอบระบบก่อนใช้จริง

1. เปิด `index.html` กรอกข้อมูลทดลอง 1 ห้อง → กด **บันทึกข้อมูล**
   ต้องขึ้นข้อความสีเขียวพร้อมรหัสอ้างอิง เช่น `RM69-00001`
2. ลองใส่จำนวนคนไม่ครบ — ระบบต้องเตือนว่าผลรวมไม่เท่ากับจำนวนนักเรียนในห้อง
3. เปิด Google Sheet ตรวจว่ามีแถวใหม่ในแผ่นงาน `ข้อมูลรายห้อง`
4. **กรอกซ้ำด้วยโรงเรียน/ปี/ภาคเรียน/ระดับชั้น/ห้องเดิม** → ต้องขึ้น
   *"ปรับปรุงข้อมูลเดิมเรียบร้อย"* และใน Sheet ต้องยังมีแถวเดียว
   โดยช่อง *แก้ไขล่าสุด* มีเวลาปรากฏ
5. เปิด `school.html` ใส่รหัสของโรงเรียนที่เพิ่งทดสอบ → ต้องเห็นแดชบอร์ด
6. เปิด `admin.html` ใส่รหัสผ่านผู้ดูแลระบบ → ต้องเห็นการ์ดสรุปและกราฟ
7. ลบแถวทดสอบออกจาก Sheet เมื่อพอใจแล้ว

---

## การใช้งานประจำ

### สำหรับครูประจำชั้น

1. เปิดลิงก์ที่โรงเรียนส่งให้
2. เลือกปีการศึกษา ภาคเรียน และวันที่ประเมิน
3. เลือกกลุ่มโรงเรียน → โรงเรียน → ระดับชั้น → ห้องที่
4. ใส่ **จำนวนนักเรียนในห้อง**
5. แต่ละทักษะ (C1–C8) อ่านคำอธิบาย 4 ระดับ แล้วใส่ว่ามีนักเรียนระดับละกี่คน
   — ป้ายข้างการ์ดจะเป็น **สีเขียว** เมื่อผลรวมตรงกับจำนวนนักเรียน
   — ใช้ปุ่ม **⤵ ใช้ค่าเหมือน C…** ช่วยคัดลอกตัวเลขจากด้านก่อนหน้าแล้วปรับแก้
6. กรอกข้อมูลผู้กรอก → **บันทึกข้อมูล** → จดรหัสอ้างอิงไว้
7. หากต้องการแก้ไข ให้กรอกใหม่ด้วยห้องเดิม ระบบจะทับข้อมูลเดิมให้

### สำหรับโรงเรียน

| ต้องการ | ทำอย่างไร |
|---|---|
| ดูภาพรวมทักษะของโรงเรียน | เปิด `school.html` ใส่รหัสโรงเรียน |
| ดูว่าห้องไหนยังไม่ส่ง | ดูป้าย *ระดับชั้นที่ยังไม่มีข้อมูล* และตารางรายห้อง |
| หาห้องที่ต้องช่วยเหลือ | ดูตารางรายห้อง — ช่องสีเข้ม/อ่อนบอกระดับคะแนน |
| ดูว่ามีเด็กกลุ่มควรพัฒนากี่คน | กราฟ *การกระจายนักเรียนตามระดับคุณภาพ รายทักษะ* |
| ทำรายงานส่งเขต | ปุ่ม **ส่งออก CSV** หรือ **พิมพ์** |

### สำหรับเขตพื้นที่

| ต้องการ | ทำอย่างไร |
|---|---|
| ดูความคืบหน้าการส่งข้อมูล | `admin.html` → แท็บ **โรงเรียนที่ยังไม่ส่ง** |
| ดูภาพรวมทักษะของเขต | แท็บ **แดชบอร์ด** — ดูการ์ด *ทักษะที่ควรเร่งพัฒนา* เพื่อวางแผนนิเทศ |
| เทียบอำเภอ / กลุ่มโรงเรียน | แท็บ **สรุประดับเขต** |
| แจกรหัสให้โรงเรียน | แท็บ **รหัสโรงเรียน** → ส่งออก CSV |
| นำข้อมูลไปทำรายงาน | ปุ่ม **ส่งออก CSV** แล้วเปิดใน Excel |
| ปิดรับข้อมูลเมื่อครบกำหนด | แก้ `OPEN_FOR_SUBMIT = false` ใน Code.gs → บันทึก → Deploy เวอร์ชันใหม่ |

---

## แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุและวิธีแก้ |
|---|---|
| ขึ้นกล่องสีเหลือง *"ยังไม่ได้ตั้งค่าระบบ"* | ยังไม่ได้ใส่ `API_URL` — กลับไปทำขั้นที่ 5 |
| กดบันทึกแล้วขึ้น *"เชื่อมต่อระบบไม่สำเร็จ"* | ตั้ง *ผู้ที่มีสิทธิ์เข้าถึง* ไม่เป็น **ทุกคน** — Deploy ใหม่ตามขั้นที่ 4 |
| ขึ้น *"จำนวนนักเรียนรวม … ไม่เท่ากับจำนวนนักเรียนในห้อง"* | ตัวเลข 4 ระดับของทักษะนั้นรวมแล้วไม่เท่ากับจำนวนนักเรียน — ดูป้ายสีแดงข้างการ์ด |
| แดชบอร์ดเขตขึ้น *"รหัสผ่านไม่ถูกต้อง"* | รหัสไม่ตรงกับ `ADMIN_PASS` ใน Code.gs (ตัวพิมพ์เล็ก-ใหญ่มีผล) |
| แดชบอร์ดโรงเรียนขึ้น *"รหัสไม่ถูกต้อง"* | พิมพ์รหัสผิด หรือ `CODE_SALT` ถูกเปลี่ยนหลังแจกรหัส — ออกรหัสใหม่จากแท็บ *รหัสโรงเรียน* |
| แก้ Code.gs แล้วไม่มีผล | ต้อง **Deploy → จัดการการทำให้ใช้งานได้ → แก้ไข ✏️ → เวอร์ชัน: ใหม่ → ทำให้ใช้งานได้** |
| เบอร์โทรใน Sheet หาย 0 ตัวหน้า | ระบบใส่เครื่องหมาย `'` ป้องกันไว้แล้ว หากยังพบ ให้ตั้งรูปแบบคอลัมน์เป็น *ข้อความ* |
| ข้อมูลซ้ำหลายแถว | เกิดจากเลือกห้องไม่ตรงกัน — ให้เลือกจากรายการเท่านั้น ห้ามพิมพ์เอง |
| เปิด CSV ใน Excel แล้วภาษาไทยเพี้ยน | ไฟล์มี BOM แล้ว หากยังเพี้ยน ให้ใช้ *ข้อมูล → จากข้อความ/CSV* แล้วเลือก UTF-8 |

---

## การปรับแต่งเพิ่มเติม

| ต้องการแก้ | แก้ที่ไหน |
|---|---|
| ข้อความรูบริกแต่ละระดับ | ตัวแปร `RUBRIC` ในไฟล์ `rubric.js` **และ** ในส่วน `<script>` ของไฟล์ HTML ทั้งสาม |
| รายชื่อโรงเรียน | ตัวแปร `SCHOOLS` ในไฟล์ HTML และใน `Code.gs` — ต้องตรงกัน |
| ระดับชั้นที่เปิดให้เลือก | ตัวแปร `GRADES` ในไฟล์ HTML และ `LEVELS` ใน `Code.gs` — ต้องตรงกัน |
| เกณฑ์แปลผลคะแนน | ฟังก์ชัน `qualityLabel()` ใน `Code.gs` และ `qualityOf()` ในไฟล์ HTML — ต้องตรงกัน |
| สีของระบบ | ตัวแปร CSS ที่ `:root` ส่วนบนของไฟล์ HTML |

> ไฟล์ HTML เป็นไฟล์เดี่ยวที่ฝังข้อมูลไว้ในตัว เมื่อแก้ `rubric.js` หรือ
> `schools.json` แล้วต้องคัดลอกไปวางทับส่วนเดิมในไฟล์ HTML ทุกไฟล์ด้วย

---

## ความปลอดภัยของข้อมูล

- ข้อมูลทั้งหมดอยู่ใน Google Sheet ของเขตพื้นที่ ไม่ผ่านบุคคลที่สาม
- ระบบเก็บเป็น **จำนวนนับรายห้อง** ไม่มีชื่อนักเรียนรายบุคคล
- หน้ากรอกข้อมูลเป็นสาธารณะ (ครูไม่ต้องล็อกอิน) แต่ **อ่านข้อมูลย้อนกลับไม่ได้**
- โรงเรียนอ่านได้เฉพาะข้อมูลของตนเองผ่านรหัสเฉพาะโรงเรียน
- การอ่านข้อมูลทั้งเขตต้องใช้ `ADMIN_PASS` เท่านั้น
- `CODE_SALT` อยู่ในฝั่งเซิร์ฟเวอร์เท่านั้น ไม่ปรากฏในไฟล์เว็บ จึงเดารหัสโรงเรียนอื่นไม่ได้
- แนะนำให้เปลี่ยน `ADMIN_PASS` ทุกปีการศึกษา และไม่ส่งต่อในกลุ่มไลน์สาธารณะ
- สำรองข้อมูลด้วย **ไฟล์ → ดาวน์โหลด → Excel** ทุกสิ้นภาคเรียน

---

กลุ่มนิเทศ ติดตาม และประเมินผลการจัดการศึกษา
สำนักงานเขตพื้นที่การศึกษาประถมศึกษาอุดรธานี เขต 1
