# คู่มือการติดตั้งและใช้งาน — ระบบเก็บข้อมูลทักษะ 8C

**สำนักงานเขตพื้นที่การศึกษาประถมศึกษาอุดรธานี เขต 1**

> 📌 ระบบติดตั้งเรียบร้อยและเปิดใช้งานแล้ว
> หากต้องการใช้งานอย่างเดียว ให้ข้ามไปที่ [**การใช้งานประจำ**](#การใช้งานประจำ)
> ส่วนขั้นตอนที่ 1–6 ใช้เมื่อต้องติดตั้งใหม่หรือย้ายไปบัญชีอื่น

---

## ภาพรวมของระบบ

| หน้า | ใครใช้ | เข้าอย่างไร |
|---|---|---|
| `index.html` — ภาพรวมระดับเขต | ทุกคน | เปิดได้เลย ไม่ต้องล็อกอิน |
| `school.html` — แดชบอร์ดโรงเรียน | ทุกคน | เปิดได้เลย ไม่ต้องล็อกอิน |
| `form.html` — กรอกข้อมูล | ครูของโรงเรียน | รหัสโรงเรียน SMIS + รหัสผ่าน |
| แท็บ *ผู้ดูแลระบบ* ในหน้าเขต | เจ้าหน้าที่เขต | รหัสผ่านผู้ดูแลระบบ |

เส้นทางการใช้งาน — **หน้าเขต → เลือกโรงเรียน → แดชบอร์ดโรงเรียน → ปุ่มเข้าสู่ระบบ → กรอกข้อมูล**

---

## ขั้นที่ 1 — สร้าง Google Sheet

1. เปิด <https://sheets.new> จะได้ไฟล์เปล่า 1 ไฟล์
2. ตั้งชื่อไฟล์ เช่น `ฐานข้อมูลทักษะ 8C สพป.อุดรธานี เขต 1`
3. ดูที่แถบ URL ด้านบน

   ```
   https://docs.google.com/spreadsheets/d/1AbCdEf...XyZ/edit#gid=0
                                          └──── ส่วนนี้คือ SHEET_ID ────┘
   ```

4. **คัดลอกรหัสระหว่าง `/d/` กับ `/edit` เก็บไว้**

ไม่ต้องสร้างแผ่นงานเอง ระบบจะสร้าง `ข้อมูลรายห้อง` (64 คอลัมน์) และ `ผู้ใช้งาน`
พร้อมหัวตารางให้อัตโนมัติ

---

## ขั้นที่ 2 — สร้างโปรเจกต์ Apps Script

1. เปิด <https://script.google.com> → **โครงการใหม่**
2. ตั้งชื่อ เช่น `8C Backend — สพป.อุดรธานี 1`
3. ลบโค้ดตัวอย่างออกให้หมด แล้ววาง `Code.gs` ทั้งไฟล์แทน
4. กด **บันทึก** (Ctrl+S)

---

## ขั้นที่ 3 — แก้ค่าตั้งต้น 4 บรรทัด

```javascript
const SHEET_ID = 'ใส่รหัส_GOOGLE_SHEET_ที่นี่';
const ADMIN_PASS = 'udn1@8c2569';
const CODE_SALT = 'udn1-8c-salt-เปลี่ยนค่านี้ด้วย';
const OPEN_FOR_SUBMIT = true;
```

| ตัวแปร | ใส่อะไร |
|---|---|
| `SHEET_ID` | รหัสจากขั้นที่ 1 |
| `ADMIN_PASS` | **เปลี่ยนเป็นรหัสผ่านของท่านเอง** สำหรับส่วนผู้ดูแลระบบของเขต |
| `CODE_SALT` | **เปลี่ยนเป็นข้อความลับของท่านเอง** ใช้เข้ารหัสรหัสผ่านของโรงเรียน |
| `OPEN_FOR_SUBMIT` | `true` = เปิดรับข้อมูล · `false` = ปิดรับชั่วคราว |

> ⚠️ **ห้ามเปลี่ยน `CODE_SALT` หลังโรงเรียนตั้งรหัสผ่านของตนเองแล้ว**
> เพราะรหัสผ่านที่ตั้งไว้จะใช้ไม่ได้ทั้งหมด ต้องรีเซ็ตใหม่ทุกโรงเรียน

### ทดสอบการเชื่อมต่อ

1. เลือกฟังก์ชัน **`ทดสอบการตั้งค่า`** → กด **เรียกใช้**
2. ครั้งแรกจะขออนุญาต — กด **ตรวจสอบสิทธิ์** → เลือกบัญชี →
   หากขึ้น *"Google ไม่ได้ยืนยันแอปนี้"* ให้กด **ขั้นสูง** → **ไปที่ … (ไม่ปลอดภัย)** → **อนุญาต**
3. ดู **บันทึกการดำเนินการ** ต้องขึ้นประมาณนี้

   ```
   ✅ เชื่อมต่อ Google Sheet สำเร็จ: ฐานข้อมูลทักษะ 8C ...
      • แผ่นงาน "ข้อมูลรายห้อง" (0 ห้อง)
      • แผ่นงาน "ผู้ใช้งาน" (0 บัญชี)
   ✅ รายชื่อโรงเรียน 20 กลุ่ม / 214 โรงเรียน
      • ตัวอย่างบัญชี: 41010001 (บ้านท่าตูมดงสระพัง) รหัสผ่านเริ่มต้น = 41010001
   ✅ ระบบเปิดรับข้อมูล
   ```

---

## ขั้นที่ 4 — Deploy เป็นเว็บแอป

1. มุมขวาบน **ทำให้ใช้งานได้** → **การทำให้ใช้งานได้ใหม่**
2. ไอคอนเฟือง ⚙️ → เลือก **เว็บแอป (Web app)**
3. ตั้งค่า **(สำคัญมาก)**

   | ช่อง | ค่าที่ต้องตั้ง |
   |---|---|
   | ดำเนินการในฐานะ | **ฉัน (Me)** |
   | ผู้ที่มีสิทธิ์เข้าถึง | **ทุกคน (Anyone)** |

   > ต้องเป็น **ทุกคน** เท่านั้น มิฉะนั้นครูที่ไม่ได้ล็อกอิน Google จะใช้ระบบไม่ได้

4. คัดลอก **URL ของเว็บแอป** ที่ลงท้ายด้วย `/exec`

---

## ขั้นที่ 5 — ใส่ URL ลงในไฟล์เว็บ

**วิธีอัตโนมัติ** (มี Python)

```bash
python3 build.py "https://script.google.com/macros/s/AKfycb....../exec"
```

**วิธีแก้มือ** — เปิด `index.html`, `school.html`, `form.html`
ด้วยโปรแกรมแก้ไขข้อความ (ห้ามใช้ Microsoft Word) แล้วแก้บรรทัด

```javascript
const API_URL = "";
```

เป็น

```javascript
const API_URL = "https://script.google.com/macros/s/AKfycb....../exec";
```

**ต้องแก้ทั้งสามไฟล์**

---

## ขั้นที่ 6 — นำไฟล์เว็บขึ้นออนไลน์ (GitHub Pages)

1. เข้า <https://github.com> → **New repository** → ตั้งชื่อ เช่น `8c-udn1` → **Public** → Create
2. **uploading an existing file** → ลาก `index.html`, `school.html`, `form.html`,
   `admin.html` เข้าไป → **Commit**
3. **Settings → Pages** → *Branch* เลือก `main` / `root` → **Save**
4. รอ 1–2 นาที จะได้ลิงก์

   ```
   https://ชื่อผู้ใช้.github.io/8c-udn1/             ← หน้าเขต (ส่งให้ทุกคน)
   https://ชื่อผู้ใช้.github.io/8c-udn1/school.html  ← แดชบอร์ดโรงเรียน
   https://ชื่อผู้ใช้.github.io/8c-udn1/form.html    ← หน้ากรอกข้อมูล
   ```

> ส่งลิงก์เดียวก็พอ — **หน้าเขต** เพราะเข้าถึงทุกอย่างได้จากตรงนั้น

---

## ก่อนเปิดใช้งานจริง — ล้างข้อมูลทดสอบ

ที่ Apps Script เลือกฟังก์ชัน **`ล้างข้อมูลทั้งหมด`** → **เรียกใช้**
ระบบจะลบข้อมูลรายห้องทุกแถว ล้างบัญชีผู้ใช้ (รหัสผ่านกลับเป็นรหัสโรงเรียน)
และลบแผ่นงานเปล่าที่ไม่ใช้

> ⚠️ ลบแล้วกู้คืนไม่ได้ — ควรสำรองก่อนด้วย **ไฟล์ → ดาวน์โหลด → Excel**
> ใช้ฟังก์ชันนี้ซ้ำได้ทุกครั้งที่ขึ้นปีการศึกษาใหม่

---

## การใช้งานประจำ

### สำหรับครูผู้กรอกข้อมูล

1. เปิดลิงก์หน้าเขต → แท็บ **รายโรงเรียน** → คลิกชื่อโรงเรียนของตน
   (หรือเปิด `school.html` แล้วค้นหาชื่อโรงเรียน)
2. กดปุ่ม **เข้าสู่ระบบกรอกข้อมูล** (ปุ่มสีส้ม)
3. ใส่ **รหัสโรงเรียน 8 หลัก** และ **รหัสผ่าน**
   — ครั้งแรกรหัสผ่านคือรหัสโรงเรียนเดียวกัน
   — ระบบจะเตือนให้เปลี่ยนรหัสผ่าน ควรเปลี่ยนทันที
4. เลือกปีการศึกษา ภาคเรียน ระดับชั้น และห้อง
   — จำนวนนักเรียนเติมให้อัตโนมัติจาก BIG DATA แก้ไขได้ตามจริง
5. แต่ละทักษะ (C1–C8) อ่านคำอธิบาย 4 ระดับ แล้วใส่ว่ามีนักเรียนระดับละกี่คน
   — ป้ายข้างการ์ดเป็น **สีเขียว** เมื่อผลรวมตรงกับจำนวนนักเรียน
   — ใช้ปุ่ม **ใช้ค่าเหมือน C…** คัดลอกจากด้านก่อนหน้าแล้วปรับแก้
6. กรอกข้อมูลผู้กรอก → **บันทึกข้อมูล** → กด **กรอกห้องถัดไป** เพื่อทำห้องต่อไป
7. แก้ไขภายหลังได้ โดยกรอกใหม่ด้วยห้องเดิม ระบบจะทับข้อมูลเดิมให้

### สำหรับโรงเรียน

| ต้องการ | ทำอย่างไร |
|---|---|
| ดูภาพรวมทักษะของโรงเรียน | เปิดแดชบอร์ดโรงเรียนได้เลย ไม่ต้องล็อกอิน |
| ดูว่ากรอกครบหรือยัง | ดูแถบ **ความคืบหน้าการกรอกข้อมูล** — เทียบกับจำนวนห้องจริง |
| หาห้องที่ต้องช่วยเหลือ | ตารางรายห้อง — ช่องสีเข้ม/อ่อนบอกระดับคะแนน |
| ดูว่ามีเด็กกลุ่มควรพัฒนากี่คน | กราฟ *การกระจายนักเรียนตามระดับคุณภาพ รายทักษะ* |
| ทำรายงาน | ปุ่ม **CSV** หรือ **พิมพ์** |
| ลืมรหัสผ่าน | แจ้งเขตพื้นที่เพื่อรีเซ็ต |

### สำหรับเขตพื้นที่

| ต้องการ | ทำอย่างไร |
|---|---|
| ดูภาพรวมทั้งเขต | หน้าเขต แท็บ **แดชบอร์ดเขต** |
| เทียบอำเภอ / กลุ่มโรงเรียน | แท็บ **รายอำเภอ / กลุ่ม** |
| ดูผลรายโรงเรียนและเข้าไปดูลึก | แท็บ **รายโรงเรียน** → คลิกชื่อโรงเรียน |
| ติดตามโรงเรียนที่ยังไม่ส่ง | แท็บ **ยังไม่ส่งข้อมูล** |
| ดูข้อมูลติดต่อครูผู้กรอก | ปุ่ม **ผู้ดูแลระบบ** → ใส่รหัสผ่าน → แท็บ **ผู้ดูแลระบบ** |
| รีเซ็ตรหัสผ่านให้โรงเรียน | แท็บ **ผู้ดูแลระบบ** → ปุ่ม **รีเซ็ต** ท้ายแถว |
| ส่งออกข้อมูลเต็ม | เข้าสู่ระบบผู้ดูแล → แท็บ **ผู้ดูแลระบบ** → ปุ่ม **CSV** |
| ปิดรับข้อมูลเมื่อครบกำหนด | แก้ `OPEN_FOR_SUBMIT = false` → บันทึก → Deploy เวอร์ชันใหม่ |
| ขึ้นปีการศึกษาใหม่ | สำรองข้อมูล แล้วรันฟังก์ชัน `ล้างข้อมูลทั้งหมด` |

---

## แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุและวิธีแก้ |
|---|---|
| หน้าเว็บขึ้น *"ยังไม่ได้ตั้งค่า API_URL"* | ยังไม่ได้ใส่ URL — กลับไปทำขั้นที่ 5 |
| ขึ้น *"เชื่อมต่อระบบไม่สำเร็จ"* | ตั้ง *ผู้ที่มีสิทธิ์เข้าถึง* ไม่เป็น **ทุกคน** — Deploy ใหม่ตามขั้นที่ 4 |
| ขึ้น *"ไม่พบรหัสโรงเรียนนี้ในระบบ"* | พิมพ์รหัส SMIS ผิด — ดูรหัสที่ถูกต้องได้ในหน้าเขต แท็บ *รายโรงเรียน* |
| ขึ้น *"หมดอายุการเข้าสู่ระบบ"* | เปลี่ยนรหัสผ่านจากอีกเครื่อง หรือถูกรีเซ็ต — เข้าสู่ระบบใหม่ |
| ขึ้น *"จำนวนนักเรียนรวม … ไม่เท่ากับจำนวนนักเรียนในห้อง"* | ตัวเลข 4 ระดับของทักษะนั้นรวมไม่เท่ากับจำนวนนักเรียน — ดูป้ายสีแดงข้างการ์ด |
| ไม่มีระดับชั้นที่ต้องการให้เลือก | BIG DATA ระบุว่าโรงเรียนไม่ได้เปิดสอนชั้นนั้น — แจ้งเขตเพื่อปรับข้อมูล |
| แก้ Code.gs แล้วไม่มีผล | ต้อง **Deploy → จัดการการทำให้ใช้งานได้ → แก้ไข ✏️ → เวอร์ชัน: ใหม่ → ทำให้ใช้งานได้** |
| เบอร์โทรใน Sheet หาย 0 ตัวหน้า | ระบบใส่ `'` ป้องกันไว้แล้ว หากยังพบ ให้ตั้งรูปแบบคอลัมน์เป็น *ข้อความ* |
| เปิด CSV ใน Excel แล้วภาษาไทยเพี้ยน | ไฟล์มี BOM แล้ว หากยังเพี้ยน ใช้ *ข้อมูล → จากข้อความ/CSV* แล้วเลือก UTF-8 |

---

## การปรับแต่งเพิ่มเติม

ไฟล์ต้นแบบอยู่ในโฟลเดอร์ `src/` แก้แล้วรัน `python3 build.py "<API_URL>"` เพื่อสร้างไฟล์ใหม่

| ต้องการแก้ | แก้ที่ไหน |
|---|---|
| ข้อความรูบริกแต่ละระดับ | `rubric.js` |
| ข้อมูลโรงเรียน / จำนวนห้อง / จำนวนนักเรียน | `schools2569.json` |
| หน้าเขต / หน้าโรงเรียน / หน้ากรอก | `src/index.template.html` · `src/school.template.html` · `src/form.template.html` |
| หลังบ้าน | `src/Code.template.gs` |
| ไอคอน | `src/icons.svg` |
| สีของระบบ | ตัวแปร CSS ที่ `:root` ส่วนบนของไฟล์ template |

> เมื่ออัปเดตปีการศึกษาใหม่ ให้ดึงข้อมูลจาก BIG DATA มาสร้าง `schools2569.json` ใหม่
> แล้ว build ใหม่ — ระบบจะอัปเดตจำนวนห้อง/นักเรียนของทุกโรงเรียนพร้อมกัน

---

## ความปลอดภัยของข้อมูล

- ข้อมูลทั้งหมดอยู่ใน Google Sheet ของเขตพื้นที่ ไม่ผ่านบุคคลที่สาม
- ระบบเก็บเป็น **จำนวนนับรายห้อง** ไม่มีชื่อนักเรียนรายบุคคล
- แดชบอร์ดเปิดสาธารณะ แต่ **ตัดเบอร์โทรศัพท์ อีเมล/ID Line และหมายเหตุออก**
  ข้อมูลติดต่อดูได้เฉพาะผู้ดูแลระบบของเขต
- โรงเรียนกรอกข้อมูลได้เฉพาะโรงเรียนของตนเอง (ระบบล็อกตามบัญชีที่เข้าสู่ระบบ)
- รหัสผ่านเก็บเป็นค่าแฮช **SHA-256** ไม่มีใครอ่านย้อนกลับได้ รวมถึงผู้ดูแลระบบ
- แนะนำให้เปลี่ยน `ADMIN_PASS` ทุกปีการศึกษา และไม่ส่งต่อในกลุ่มไลน์สาธารณะ
- แนะนำให้โรงเรียนเปลี่ยนรหัสผ่านจากค่าเริ่มต้นตั้งแต่เข้าใช้ครั้งแรก
- สำรองข้อมูลด้วย **ไฟล์ → ดาวน์โหลด → Excel** ทุกสิ้นภาคเรียน

---

กลุ่มนิเทศ ติดตาม และประเมินผลการจัดการศึกษา
สำนักงานเขตพื้นที่การศึกษาประถมศึกษาอุดรธานี เขต 1
